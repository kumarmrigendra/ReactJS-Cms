import React from 'react';
import { Route, IndexRoute } from 'react-router';
import components from './components';
const quoteManagement = (	
	<Route path = "quotes" component = {components}>
	</Route>
);
export default quoteManagement;
